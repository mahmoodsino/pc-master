/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
}

module.exports = {
  reactStrictMode: true,
  images: {
      domains: ["pc-master-back.para-store.net"],
      formats: ["image/webp"],
  },
};